import React from 'react';
import { Container } from 'react-bootstrap';
import HeroArea from '../heroArea/HeroArea';

const Home = () => {
    return (
        <div >
            <Container>
                <HeroArea></HeroArea>
           </Container>
        </div>
    );
};

export default Home;